require "Utils/animationManager"
require "animatedEntity"
require "Direction"
require "Weapon/weapon"
require "Player/player"

playerWalkState = class_extends(playerAliveState, "idle")


function playerWalk.load()
  
end

function playerWalk:update(dt)
  
end

function playerWalk:draw(of)

end
